// Para probar desde un celular real, cambia localhost por la IP que imprime el
// backend al arrancar (por ejemplo http://192.168.1.15:3000).
export const environment = {
  production: false,
  apiUrl: 'http://localhost:3000',
};
