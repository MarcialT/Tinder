export const environment = {
  production: true,
  // IP de la red local del computador donde corre el backend (npm start en /backend).
  // Si cambia de red WiFi, actualiza esta IP y vuelve a compilar el APK.
  apiUrl: 'http://192.168.68.100:3000',
};
